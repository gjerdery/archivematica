<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ feed.url }}</id>
  <title type="text">{{ feed.title }}</title>
  <link href="{{ feed.url }}" rel="self">
  </link>
  {% for item in items %}
  <entry>
    <id>{{ item.url }}</id>
    <title type="text">{{ item.title }}</title>
    <link href="{{ item.url }}" rel="self">
    </link>
  </entry>
  {% endfor %}
</feed>
